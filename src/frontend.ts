/**
 * ~(*≧ω≦) Autogenerates an `index.html` probably skill issue though ☝😈
 * 
 * - I focus on **backend logic and app fwoooow things~** so this HTML is **autogenerated~**  
 *   instead of wasting my time with nerdy YUAI YUEKS design! >:3  
 * - This minimal template covers all elements for now:
 *   - 🌸 Registering accounts  
 *   - 🎀 Selecting and removing accounts  
 *   - 💻 Auto-login magic! ✨  
 * - The design is **dark mode supremacy** because light mode is for normies.  
 * - The generated file is saved as `resources/index.html` like a good little script! (*≧▽≦)  
 * - Logs success if it works, and if not... ugh, I guess it’s **yabai time**. 💀
 * - Hnnnnnnng, maybe later I should use Nuxt or Next.js... but for now, it's just **plain vanilla**. ☝😔
 * - You should do PR if u can fix me senpai~! ☝🥺
 * 
 * - This bundle is mandatory for the app to work: Let me know if you want moar desu~! 🍦😩 
 *   - 📦 `<script src="js/neutralino.js"></script>` → Loads the Neutralino magic lib!  
 *   - 📦 `<script src="js/app.js"></script>` → Loads compiled files and handles the app’s cute flows. 💖  
 * 
 * @requires fs - Node.js File System module for saving the file~ ✨  
 * @throws {Error} Logs a big fat **yabai time**. 💀 if file writing fails. (╯°□°）╯︵ ┻━┻  
 */


// eslint-disable-next-line @typescript-eslint/no-explicit-any
declare const require: any;
const fs = require("fs");

const htmlContent = `
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Index</title>
    <link rel="icon" type="image/x-icon" href="/icons/favicon.ico">
    <style>
body {
    font-family: Arial, sans-serif;
    background-color: #1e1e2e;
    color: #ffffff;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    margin: 0;
}
.container {
    width: 90%;
    max-width: 400px;
    background: #2a2a3b;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}
textarea {
    width: 100%;
    height: 100px;
    background: #333344;
    color: white;
    border: none;
    padding: 10px;
    border-radius: 5px;
    resize: none;
    box-sizing: border-box; /* Fix ukuran agar padding tidak bikin ukuran kacau */
}
select, button {
    width: 100%;
    padding: 10px;
    margin-top: 10px;
    font-size: 16px;
    border: none;
    border-radius: 5px;
}
select {
    background: #444455;
    color: white;
}
#registerBtn {
    background: #007bff;
    color: white;
}
#registerBtn:hover {
    background: #529be8;
}
#runBtn {
    background: #28a745;
    color: white;
}

#runBtn:hover {
    background: #68e986;
}
#removeBtn {
    background: #a20a0a; 
    color: white;
}
#removeBtn:hover {
    background: #d74141;
}

.responsive-img {
    width: 100%;
    height: auto;
    max-width: 400px;
    display: block;
    margin: 0 auto; 
}
#selectPathBtn {
    margin-left: 3px;
    background-color: #007bff; 
    color: white;
    font-size: 12px;
    border: none;
    padding: 3px 9px; 
    cursor: pointer;
    border-radius: 3px;
    width: auto; 
    min-width: 50px; 
}
#selectPathBtn:hover {
    background-color: #529be8;
}
#pathContainer {
    display: flex;
    align-items: center;
    gap: 5px;
}
#selectedPath {
    font-family: monospace;
    white-space: nowrap; 
    overflow: hidden;
    text-overflow: ellipsis; 
}
</style>
</head>
<body>
    <div class="container">
        <img src="/icons/logo.png" alt="Description" class="responsive-img">
        <div id="pathContainer" style="display: flex; align-items: center;">
            <pre id="selectedPath">Please select where nikke_launcher.exe was installed.</pre>
            <button id="selectPathBtn">Edit</button>
        </div>
        <textarea id="jsonInput" placeholder="Enter account JSON here..."></textarea>
        <button id="registerBtn">🔑Register Accounts</button>
        <br><br>
        <select id="accountSelect">
            <option value="">Select account</option>
        </select>
        <button id="runBtn">Proceed auto login</button>
        <br>
        <button id="removeBtn">or Remove account</button>
    </div>
    <p>Password Manager for NIKKE v<span id="appVersion">Loading...</span></p>
    <script src="js/neutralino.js"></script>  
    <script src="js/app.js"></script>
</body>
</html>
`;

try {
  fs.writeFileSync("resources/index.html", htmlContent, "utf-8");
  console.log("✅ index.html created successfully.");
} catch (error) {
  console.error("❌ Failed to create index.html:", error);
}
