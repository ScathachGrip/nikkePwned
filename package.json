{
  "name": "nikkepwned",
  "version": "5.0.14",
  "description": "Password Manager for NIKKE",
  "keywords": [],
  "author": "sinkaroid",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ScathachGrip/nikkepwned.git"
  },
  "type": "module",
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/bun": "^1.4.0",
    "svelte": "^5.0.0",
    "typescript": "^7.0.2",
    "vite": "^6.0.0"
  },
  "scripts": {
    "style:gen": "rustc src-tauri/src/bin/style_gen.rs -o style_gen.exe && ./style_gen.exe",
    "build:ui": "bun run style:gen && vite build",
    "build:debug": "bun run build:ui && cd src-tauri && cargo build",
    "build:release": "bun run build:ui && cd src-tauri && cargo build --release",
    "build": "bun run build:release",
    "dev": "bun run style:gen && vite",
    "tauri:dev": "bun run build:ui && cd src-tauri && cargo tauri dev",
    "lint": "tsc --noEmit",
    "chore:release": "bun run src/dev_tools/chore_release.ts"
  },
  "packageManager": "bun@1.4.0",
  "engines": {
    "bun": ">=1.4.0"
  }
}